  <%# start navbar %>
    <nav class="navbar navbar-expand-md navbar-dark bg-dark">
        <div class="container ">
         <a class="navbar-brand" href="/">Shop</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                  <span class="navbar-toggler-icon"></span>
                </button>
              
                <div class="collapse navbar-collapse " id="navbarSupportedContent">
                  <ul class="navbar-nav mr-auto">
                    <li class="nav-item <%= title === "Home" ? "active" : "" %> ">
                      <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
                    </li>
                    <%if (isuser) { %>
                      <li class="nav-item <%= title === "Cart" ? "active" : "" %> ">
                        <a class="nav-link" href="/card">Cart</a>
                      </li>
                      <li class="nav-item <%= title === "Orders" ? "active" : "" %> ">
                        <a class="nav-link" href="/order">Orders</a>
                      </li>
                    <% } %>
                    <%if (isAdmin) { %>
                     <li class="nav-item <%= title === "Add Product" ? "active" : "" %>" >
                      <a class="nav-link" href="/admin/add">Add Product</a>
                    </li>
                     <li class="nav-item <%= title === "Mange Order" ? "active" : "" %> " >
                      <a class="nav-link" href="/admin/manage">Manages Orders</a>
                    </li>
                    <% } %>
                  </ul>
                  <%if (isuser) { %>
                    <form action="/logout" method="post">
                      <input type="submit" value="Logout" class="btn btn-danger">
                    </form>
                  <% } else { %>
                    <ul class="navbar-nav ">
                      <li class="nav-item  <%= title === "Signup" ? "active" : "" %>  ">
                        <a class="nav-link " href="/signup">Sign up</a>
                      </li>
                      <li class="nav-item <%= title === "Login" ? "active" : "" %>  ">
                        <a class="nav-link" href="/login">Login</a>
                      </li>
                    </ul>
                  <% } %>
                </div>
        </div>
               
    </nav>
    <%# end navbar%>